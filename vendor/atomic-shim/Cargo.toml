# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
edition = "2018"
name = "atomic-shim"
version = "0.2.0"
authors = ["Bruno Tavares <connect+github@bltavares.com>"]
description = "Atomic types for unsupported platforms"
homepage = "https://github.com/bltavares/atomic-shim"
readme = "README.md"
keywords = ["atomic"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/bltavares/atomic-shim"
[package.metadata.docs.rs]
features = ["mutex"]
[dependencies.crossbeam-utils]
version = "0.8"
optional = true

[features]
mutex = ["crossbeam-utils"]
[target."cfg(target_arch = \"mips\")".dependencies.crossbeam-utils]
version = "0.8"
[target."cfg(target_arch = \"powerpc\")".dependencies.crossbeam-utils]
version = "0.8"
