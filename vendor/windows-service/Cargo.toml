# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.60.0"
name = "windows-service"
version = "0.8.0"
authors = ["Mullvad VPN"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A crate that provides facilities for management and implementation of windows services"
readme = "README.md"
keywords = [
    "windows",
    "service",
    "daemon",
]
categories = [
    "api-bindings",
    "os::windows-apis",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/mullvad/windows-service-rs"

[package.metadata.docs.rs]
default-target = "x86_64-pc-windows-msvc"

[lib]
name = "windows_service"
path = "src/lib.rs"

[[example]]
name = "install_service"
path = "examples/install_service.rs"

[[example]]
name = "notify_service"
path = "examples/notify_service.rs"

[[example]]
name = "pause_continue"
path = "examples/pause_continue.rs"

[[example]]
name = "ping_service"
path = "examples/ping_service.rs"

[[example]]
name = "service_config"
path = "examples/service_config.rs"

[[example]]
name = "service_failure_actions"
path = "examples/service_failure_actions.rs"

[[example]]
name = "uninstall_service"
path = "examples/uninstall_service.rs"

[target."cfg(windows)".dependencies.bitflags]
version = "2.3"

[target."cfg(windows)".dependencies.widestring]
version = "1"

[target."cfg(windows)".dependencies.windows-sys]
version = "0.59.0"
features = [
    "Win32_Foundation",
    "Win32_Storage_FileSystem",
    "Win32_System_Power",
    "Win32_System_RemoteDesktop",
    "Win32_System_Services",
    "Win32_System_SystemServices",
    "Win32_System_Threading",
    "Win32_System_WindowsProgramming",
    "Win32_UI_WindowsAndMessaging",
]
